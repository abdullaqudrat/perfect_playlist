<h1><%= @artist.name %></h1>

<% @artist.songs.each do |song| %>
  <%= song.title %>
<% end %>

<%= link_to 'edit', edit_artist_path(@artist) %>
<%= link_to 'delete', artist_path(@artist), method: :delete, data: {confirm: "Really delete #{@artist}?"} %>

<%= form_for @song do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :artist_id %>
  <%= f.collection_select(:artist_id, @artists, :id, :name) %>

  <%= f.label :length %>
  <%= f.text_field :length %>

  <%= f.label :bpm %>
  <%= f.number_field :bpm %>

  <%= f.label :genre %>
  <%= f.text_field :genre %>

  <%= f.label :link %>
  <%= f.text_field :link %>

  <%= f.submit %>

<% end %>

<h1>All Songs</h1>

<h3><%= link_to "Create a New Song", new_song_path %></h3>

<% @songs.each do |song| %>
  <div id="song-<%=song.id%>">
    <p><%= link_to song.title, song_path(song) %></p>
  </div>
<% end %>
